/**
 * Stub for $lib/core/db
 *
 * The real db module (Prisma) was removed during the Convex migration.
 * This stub exists so vi.mock('$lib/core/db') calls in unit tests can
 * resolve the module. Tests provide their own mock implementations via
 * vi.mock() factory functions.
 */
export const db = {};
