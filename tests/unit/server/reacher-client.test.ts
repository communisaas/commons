import { describe, it } from 'vitest';

describe.skip('reacher-client', () => {
	it('deferred until deleted Reacher client helper is restored or replaced by the current email verification path', () => {});
});
