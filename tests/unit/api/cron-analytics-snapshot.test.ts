import { describe, it } from 'vitest';

describe.skip('GET /api/cron/analytics-snapshot', () => {
	it('deferred until deleted analytics cron route is restored or replaced by the current scheduler path', () => {});
});
