import { describe, it } from 'vitest';

describe.skip('oauth-security', () => {
	it('deferred until deleted OAuth security helper is restored or rewritten against current auth modules', () => {});
});
