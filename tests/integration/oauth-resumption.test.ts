import { describe, it } from 'vitest';

describe.skip('OAuth resumption integration', () => {
	it('deferred until deleted DB fixture exports are rewritten for the current Convex-backed auth flow', () => {});
});
